# URL of the deployed Cloudflare Worker.
# Leave empty in local dev (Vite proxies /api/* to localhost:8787).
# Set this in Cloudflare Pages environment variables for production.
VITE_API_URL=https://f1-fantasy-api.your-account.workers.dev
