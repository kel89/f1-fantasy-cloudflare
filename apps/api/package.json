{
  "name": "@f1/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "migrate:local": "wrangler d1 migrations apply f1-fantasy-db --local",
    "migrate:remote": "wrangler d1 migrations apply f1-fantasy-db --remote"
  },
  "dependencies": {
    "@f1/shared": "workspace:*",
    "@hono/zod-validator": "^0.4.3",
    "hono": "^4.7.4",
    "jose": "^5.10.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250309.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "wrangler": "^3.109.2"
  }
}
